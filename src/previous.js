export default previos
/**
 * This method will perform addition operation.
 *
 * @param {Array} array - the array
 * @return {*} - last item in the array
 */
function previos(array) {
  return array[array.length - 2]
}
